<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Progreso3.Views.PaisView"
             Title="PaisView">
    <ContentPage.BindingContext>

    </ContentPage.BindingContext>

    <StackLayout Padding="10">
        <Button Text="Obtener Países" Command="{Binding ObtenerPaisesCommand}" />
        <Label Text="{Binding Message}" FontSize="Medium" TextColor="Green" />

        <CollectionView ItemsSource="{Binding Paises}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Horizontal" Padding="5">
                        <Label Text="{Binding Nombre}" FontSize="Medium" />
                        <Label Text="{Binding Codigo}" FontSize="Medium" TextColor="Gray" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>